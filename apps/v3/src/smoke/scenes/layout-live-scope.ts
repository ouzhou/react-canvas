import { Text, View } from "@react-canvas/react-v2";
import React from "react";

/** 供 layout 示例 {@link LiveProvider} 内联片段求值（不挂载 LivePreview，仅校验）。 */
export const layoutDemoLiveScope = { React, View, Text };
