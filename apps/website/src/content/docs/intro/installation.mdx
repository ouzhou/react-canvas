---
title: 安装与开发
description: 在 monorepo 中安装依赖并运行文档站。
---

## 依赖

在仓库根目录使用 Vite+ 提供的包管理封装安装依赖（见仓库根 `AGENTS.md`）。

## 运行文档站

```bash
vp run website#dev
```

浏览器访问终端输出的本地 URL。

## 生产构建

```bash
cd apps/website && vp build
```
