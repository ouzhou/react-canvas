---
title: Phase 1 playground
description: Live demo of CanvasProvider, Canvas, and nested View layout (Yoga + CanvasKit).
---

import { Phase1Playground } from "../../../components/Phase1Playground.tsx";

Client-side demo：CanvasKit WASM 与依赖的 `canvaskit-wasm` 版本一致，由打包器从本地 `node_modules` 解析（不再走 CDN）。

<Phase1Playground client:load />
