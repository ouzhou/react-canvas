---
title: "@react-canvas/ui"
description: CanvasThemeProvider、Design Token 与 Button、Checkbox、Switch、Avatar 等组件演示。
---

import { UiPlayground } from "../../../components/UiPlayground.tsx";

`@react-canvas/ui` 提供主题、`mergeViewStyles` / `resolveSx` 与基于 token 的组件（`Button`、`Divider`、`Checkbox`、`Switch`、`Avatar` 等）。本页在文档站中增加独立入口，画布内需同时包在 `CanvasThemeProvider` 与 `CanvasProvider` 内。

<UiPlayground client:load />
